小路书 页面级 PRD v1.0

## 1. 项目定义

**产品名称**：小路书  
**产品定位**：面向北京年轻用户的本地生活路线管家，帮助用户把“想吃饭、想玩、想放松”的模糊需求，转成可直接出门执行的路线。  
**MVP 目标**：验证用户是否愿意把下班后/周末半天的本地生活决策交给小路书。  
**首版试点**：北京，优先覆盖国贸、三里屯、望京、五道口、西单、王府井等高密度商圈。

**首版不做**：
- 不做团购购买、支付、退款。
- 不做多人协作。
- 不做跨城多日旅行。
- 不承诺所有商家的实时排队数据。
- 电话 Agent 不代用户下单，不冒充用户完成复杂预约，只做营业和等位风险确认。

---

## 2. 核心用户路径

主链路：

```text
首次进入
→ 基础画像配置
→ 首页输入本地生活需求
→ 本地生活意图识别
→ 需求补齐
→ 每个类目推荐 3 个 POI
→ 用户选择 POI
→ 生成路线
→ 后台电话核验风险
→ 路线页异步更新核验结果
→ 用户决定继续、替换或导航
→ 路线完成
→ 历史记录沉淀，后台学习偏好
```

示例：

```text
用户：今晚 18:30 从国贸出发，想吃烤肉，再去唱歌，人均 150 左右，打车。

系统：
1. 识别为本地生活路线需求。
2. 判断信息完整，无需继续追问。
3. 推荐 3 家烤肉 + 3 家 KTV。
4. 用户选择商户。
5. 系统生成“国贸 → 烤肉店 → KTV → 回家/结束点”路线。
6. 后台电话确认商家是否营业、是否等位、预计等多久。
7. 路线页展示“烤肉店已确认营业，预计等位 10 分钟；KTV 未接通，用户决定是否保留”。
```

---

## 3. 页面清单

| 页面 | 目标 | 优先级 |
|---|---|---:|
| 启动/画像配置页 | 建立冷启动画像 | P0 |
| 首页 | 发起路线需求 | P0 |
| 需求补齐页 | 补齐可规划条件 | P0 |
| POI 候选选择页 | 用户选择每类地点 | P0 |
| 路线生成中页 | 反馈计算状态 | P0 |
| 路线结果页 | 执行路线、查看风险、跳转导航 | P0 |
| 电话核验状态组件 | 展示营业/等位风险 | P0 |
| 局部退出重选页 | 替换不满意 POI | P0 |
| 历史路线页 | 复用路线、沉淀行为 | P1 |
| 我的偏好页 | 管理基础配置 | P1 |

---

## 4. 启动/画像配置页

### 页面目标

让系统在冷启动时理解用户的消费习惯，减少第一次推荐偏差。

### 触发条件

- 用户首次打开 App。
- 用户未完成基础画像配置。
- 用户在“我的偏好”中主动重新配置。

### 页面结构

```text
顶部：欢迎文案
中部：分段画像问题
底部：下一步 / 跳过 / 完成
```

### 字段设计

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| 常驻城市 | 单选 | 是 | MVP 默认北京 |
| 常用出发地 | 地址输入/定位 | 否 | 公司、学校、住处 |
| 常去商圈 | 多选 | 否 | 国贸、三里屯、望京等 |
| 人均预算 | 单选 | 是 | 50 以下、50-100、100-200、200+ |
| 交通偏好 | 多选 | 是 | 打车、地铁、步行、骑行、不限 |
| 排队容忍度 | 单选 | 是 | 不想等、15 分钟内、30 分钟内、值得就等 |
| 步行容忍度 | 单选 | 是 | 500m、1km、2km、不限 |
| 餐饮偏好 | 多选 | 否 | 烤肉、火锅、日料、川菜、烧烤等 |
| 娱乐休闲偏好 | 多选 | 否 | KTV、电影、酒吧、足疗、展览等 |

### 产品规则

- 首次只展示核心字段，避免问卷过长。
- 城市、预算、交通、排队容忍度、步行容忍度必须有默认值。
- 用户跳过后，系统使用默认画像：北京、预算 100-200、打车/地铁、不想等太久。
- 用户后续行为会后台更新偏好，不展示复杂算法标签。

### 异常状态

- 拒绝定位：允许手动输入出发地。
- 未选择预算：默认“100-200”。
- 未选择交通：默认“不限”。

### 验收标准

- 用户 1 分钟内可完成基础画像。
- 跳过画像不影响进入首页。
- 画像信息能参与后续 POI 排序。

---

## 5. 首页

### 页面目标

让用户快速表达“我现在想怎么安排”。

### 页面结构

```text
顶部：当前定位 + 切换按钮
主体：大输入框
快捷入口：吃饭 / 唱歌 / 电影 / 足疗 / 逛展 / 咖啡
下方：最近路线
底部：我的 / 历史
```

### 输入框

占位文案：

```text
想去哪儿？比如：今晚从国贸出发，吃烤肉再去唱歌
```

### 快捷入口规则

- 点击“吃饭”：进入需求补齐页，预填类目为餐饮。
- 点击“唱歌”：预填娱乐-KTV。
- 点击“足疗”：预填美容休闲-足疗。
- 点击“逛展”：预填景点/展览。
- 快捷入口不直接生成路线，仍需补齐时间、位置、预算等字段。

### 意图识别规则

系统先判断用户输入是否属于本地生活路线需求。

支持：
- 餐饮
- 娱乐
- 景点/展览/商圈
- 美容休闲
- 多 POI 串联
- 下班后、周末半日、本地即时出行

不支持：
- 写论文
- 泛知识问答
- 跨城多日旅行
- 购物比价
- 纯导航到单一点且无本地生活决策需求

### 非本地生活拒答

文案：

```text
我现在主要帮你安排本地生活路线，比如吃饭、唱歌、逛展、足疗和周末半日安排。
你可以试试：今晚从国贸出发，想吃日料再去 KTV。
```

### 验收标准

- 用户输入后 1 秒内完成意图识别反馈。
- 非本地需求不进入路线流程。
- 本地生活需求进入需求补齐页。

---

## 6. 需求补齐页

### 页面目标

把模糊需求补齐成路线规划所需的结构化条件。

### 页面结构

```text
顶部：用户原始需求摘要
中部：待确认条件卡片
底部：开始推荐按钮
```

### 结构化字段

| 字段 | 来源 | 缺失时处理 |
|---|---|---|
| 出发位置 | 定位/用户输入 | 追问 |
| 开始时间 | 用户输入/默认现在 | 默认现在，可修改 |
| 截止时间 | 用户输入 | 可跳过 |
| 人数 | 用户输入 | 默认 1 人 |
| 类目 | LLM 提取 | 追问 |
| 小类 | LLM 提取/用户选择 | 必须补齐 |
| 预算 | 用户输入/画像 | 使用画像默认，可修改 |
| 交通方式 | 用户输入/画像 | 使用画像默认，可修改 |
| POI 顺序 | 用户表达 | 未说明则系统优化 |

### 类目与小类

| 大类 | 小类示例 |
|---|---|
| 餐饮 | 烤肉、火锅、日料、川菜、烧烤、西餐、咖啡 |
| 娱乐 | KTV、电影、桌游、酒吧、Livehouse |
| 景点 | 展览、公园、商圈、博物馆、夜景 |
| 美容休闲 | 足疗、按摩、美甲、美容、SPA |

### 追问策略

只问影响结果的关键问题。

示例：
- 用户说“晚上想吃饭再唱歌”，系统问：“想吃哪类？预算大概多少？从哪里出发？”
- 用户说“从国贸出发吃烤肉再 KTV，人均 150”，系统只问：“几点开始？几个人？交通方式？”

### 开始推荐按钮状态

- 必填字段完整：按钮可用。
- 必填字段缺失：按钮置灰，并标出缺失项。
- 用户输入超出试点城市：提示当前重点支持北京。

### 验收标准

- 系统不重复追问已识别的信息。
- 用户能在 3 次交互内完成补齐。
- 补齐完成后生成标准 `RouteIntent`。

---

## 7. POI 候选选择页

### 页面目标

让用户在每个类目中选择一个满意 POI。

### 页面结构

```text
顶部：路线需求摘要
中部：类目分组候选
底部：已选 POI + 生成路线按钮
```

### 每个类目展示规则

- 每类展示 3 个候选。
- 用户必须为每个类目选择 1 个。
- 候选按综合得分排序，不只按距离或评分排序。
- 如果用户表达明确顺序，则候选按该顺序分组展示。

### POI 卡片字段

| 字段 | 说明 |
|---|---|
| 名称 | 商户/景点名称 |
| 图片 | 默认取 POI 图或占位图 |
| 距离 | 从当前路线前序节点计算 |
| 预计通勤 | 高德路径估算 |
| 人均/价格 | 餐饮人均、服务价格、门票 |
| 评分 | 美团/点评/高德等可用评分 |
| 口碑摘要 | 从评论或标签提炼 |
| 营业状态 | 根据营业时间预估 |
| 推荐理由 | 1 句话解释为什么推荐 |
| 风险标签 | 排队、较贵、远、需确认、快打烊 |

### 推荐排序因子

综合得分由以下因素组成：

```text
用户偏好匹配
+ 距离/通勤时间
+ 预算匹配
+ 营业时间可行性
+ 评分/口碑
+ 排队风险预估
+ 与前后节点顺路程度
```

### 换一批

用户点击“不喜欢，换一批”时，必须选择或输入原因：

- 太贵
- 太远
- 可能排队
- 品类不对

系统根据原因调整筛选条件：
- 太贵：降低预算权重上限。
- 太远：缩小搜索半径。
- 可能排队：提高低拥挤/可电话确认权重。
- 品类不对：回到小类选择。

### 生成路线按钮

- 所有类目已选：可点击。
- 有类目未选：置灰。
- 点击后创建路线任务，进入路线生成中页。

### 验收标准

- 每个类目稳定返回 3 个候选；不足 3 个时展示原因。
- 用户能看到每个候选的优点和风险。
- 用户选择行为进入后台偏好学习。

---

## 8. 路线生成中页

### 页面目标

在路线计算与电话核验启动期间，给用户明确反馈，避免等待焦虑。

### 页面状态

```text
1. 正在计算 POI 顺序
2. 正在计算通勤时间
3. 正在生成路线时间轴
4. 正在后台确认商家风险
```

### 产品规则

- 路线生成不等待电话核验完成。
- 电话核验任务异步执行。
- 路线计算成功后立即进入路线结果页。
- 电话核验结果通过路线页组件更新。

### 异常处理

| 异常 | 处理 |
|---|---|
| 路线 API 失败 | 提示重试或切换交通方式 |
| 通勤时间过长 | 提示“当前路线较绕，可重新选择” |
| 截止时间不可行 | 建议减少一个 POI 或缩短停留 |
| POI 已打烊 | 标记高风险并建议替换 |
| 候选坐标缺失 | 剔除该 POI，补充候选 |

### 验收标准

- 正常情况下 5 秒内进入路线结果页。
- 路线失败时必须给用户下一步选择。
- 电话核验不阻塞路线展示。

---

## 9. 路线结果页

### 页面目标

让用户能直接按照路线出门，并清楚知道每个节点的风险。

### 页面结构

```text
顶部：路线总览
中部：时间轴
下部：地图
底部：导航 / 替换 / 保存
```

### 路线总览字段

| 字段 | 说明 |
|---|---|
| 路线标题 | 如“国贸下班烤肉 + KTV” |
| 路线标签 | 顺路、少排队、预算适中、需确认 |
| 总耗时 | 含停留和通勤 |
| 总通勤 | 各段通勤合计 |
| 预计结束时间 | 根据开始时间推算 |
| 人均预算 | POI 价格估算 |
| 风险状态 | 无风险、待确认、有等待风险 |

### 时间轴节点字段

| 字段 | 说明 |
|---|---|
| 到达时间 | 预计到达 POI 时间 |
| POI 名称 | 已选地点 |
| 停留时长 | 系统默认，可调整 |
| 核验状态 | 核验中/已确认/未接通/等位/不营业 |
| 到下一站方式 | 打车/地铁/步行 |
| 到下一站耗时 | 高德估算 |
| 操作 | 导航、查看详情、替换 |

### 停留时长默认值

| 类目 | 默认停留 |
|---|---:|
| 正餐 | 90 分钟 |
| 咖啡 | 45 分钟 |
| KTV | 120 分钟 |
| 电影 | 按场次/默认 150 分钟 |
| 展览 | 90 分钟 |
| 公园/商圈 | 60 分钟 |
| 足疗/按摩 | 90 分钟 |
| 美甲/美容 | 90-120 分钟 |

### 用户操作

- 开始导航：跳转高德/系统地图。
- 保存路线：保存到历史路线。
- 替换最后一个地点：进入局部退出重选页。
- 查看核验详情：展开电话 Agent 结果。
- 重新规划：回到需求补齐页，保留原始需求。

### 验收标准

- 用户能看到完整时间轴。
- 每个 POI 都有明确风险状态。
- 用户能从路线页直接开始导航。
- 用户能处理未接通/等位过久/不营业等问题。

---

## 10. 电话核验状态组件

### 组件目标

把“不确定的商家风险”转成用户可理解、可决策的状态。

### 触发规则

- 餐饮、KTV、足疗、美容等商户默认触发。
- 景点/商圈默认不电话核验，优先使用营业时间和公开数据。
- 用户可关闭某个 POI 的核验。

### 电话 Agent 询问脚本

```text
您好，我想确认一下：
今天 X 点左右，X 位到店，店里现在正常营业吗？
如果到店的话需要等位吗？
大概需要等多久？
```

### 状态表

| 状态 | 展示文案 | 用户动作 |
|---|---|---|
| 核验中 | 正在确认商家实时情况 | 可先查看路线 |
| 已确认，无需等位 | 已确认营业，预计无需等位 | 保留 |
| 已确认，需等位 | 已确认营业，预计等位 X 分钟 | 保留或替换 |
| 未接通 | 商家暂未接通，风险未确认 | 用户决定是否保留 |
| 不营业 | 当前时间不营业 | 建议替换 |
| 信息不明确 | 商家回复不明确 | 标记需用户自行确认 |

### 未接通处理

系统不自动删除 POI。  
展示：

```text
这家店暂时没有接通电话，营业和等位情况未确认。你可以继续保留，也可以换一家更稳的。
```

按钮：
- 保留此地点
- 换一家
- 稍后再确认

### 验收标准

- 电话结果能异步更新路线页。
- 用户始终知道该 POI 是否被确认。
- 未接通不导致路线自动变化。

---

## 11. 局部退出重选页

### 页面目标

让用户替换不满意的后段 POI，同时保持前序路线稳定。

### 核心规则

- 只允许从最后一个 POI 往前退出。
- 退出某个 POI 时，该 POI 之后的节点全部取消。
- 该 POI 之前的节点固定。
- 新候选必须基于固定前序节点重新计算距离和时间。

### 示例

当前路线：

```text
公司 → 烤肉店 → KTV
```

用户替换 KTV：
- 保留公司和烤肉店。
- 重新推荐烤肉店附近/顺路的 KTV。

用户替换烤肉店：
- 保留公司。
- 烤肉店和 KTV 一起回退。
- 重新推荐餐饮，再推荐后续娱乐。

### 页面结构

```text
顶部：当前保留路线
中部：被替换节点
下方：不满意原因
底部：新候选列表
```

### 不满意原因

- 太贵
- 太远
- 排队风险高
- 不想要这个品类

### 重新推荐规则

| 原因 | 推荐策略 |
|---|---|
| 太贵 | 降低价格上限 |
| 太远 | 缩小半径，提高通勤权重 |
| 排队风险高 | 优先低拥挤、可确认商户 |
| 品类不对 | 回到小类选择 |

### 验收标准

- 替换不会打乱前面已确认节点。
- 新候选展示与前序节点的通勤时间。
- 替换后重新生成路线并重新触发必要核验。

---

## 12. 历史路线页

### 页面目标

让用户复用过去路线，并为偏好学习提供数据。

### 展示字段

- 路线标题
- 使用时间
- 起点
- 主要 POI
- 是否导航
- 是否保存
- 是否完成

### 操作

- 再来一次
- 删除记录
- 查看详情

### 复用规则

- 复用历史路线时，不直接沿用旧营业和等位状态。
- 必须重新计算路线和核验风险。
- 如果 POI 已不可用，提示替换。

### 验收标准

- 用户能找到最近路线。
- 历史路线可重新生成。
- 复用行为进入偏好学习。

---

## 13. 我的偏好页

### 页面目标

提供基础配置修改入口，但不让用户管理复杂算法标签。

### 可修改项

- 常驻城市
- 常用地点
- 默认预算
- 默认交通方式
- 排队容忍度
- 步行容忍度
- 常去商圈
- 兴趣偏好

### 不展示

- 不展示“系统认为你喜欢什么”的复杂标签。
- 不提供大量手动纠错。
- 不暴露算法权重。

### 验收标准

- 用户能修改影响推荐的基础配置。
- 修改后下一次路线推荐生效。

---

## 14. 核心数据对象

### UserProfile

```json
{
  "user_id": "string",
  "city": "北京",
  "home_locations": [],
  "budget_level": "100-200",
  "transport_preferences": ["taxi", "metro"],
  "queue_tolerance_minutes": 15,
  "walking_tolerance_meters": 1000,
  "food_preferences": ["烤肉", "日料"],
  "leisure_preferences": ["KTV", "足疗"]
}
```

### RouteIntent

```json
{
  "raw_text": "今晚从国贸出发，想吃烤肉再去唱歌",
  "city": "北京",
  "start_location": {},
  "start_time": "2026-05-11 18:30",
  "end_time": null,
  "party_size": 2,
  "budget_per_person": 150,
  "transport_mode": "taxi",
  "category_intents": [
    {"category": "餐饮", "subcategory": "烤肉"},
    {"category": "娱乐", "subcategory": "KTV"}
  ],
  "order_fixed": true
}
```

### POICandidate

```json
{
  "poi_id": "string",
  "name": "string",
  "category": "餐饮",
  "subcategory": "烤肉",
  "location": {},
  "price_per_person": 130,
  "rating": 4.6,
  "business_hours": "11:00-23:00",
  "recommend_reason": "预算匹配，距离国贸 12 分钟",
  "risk_tags": ["可能排队", "需电话确认"]
}
```

### RoutePlan

```json
{
  "route_id": "string",
  "summary": {},
  "stops": [
    {
      "poi_id": "string",
      "arrival_time": "18:45",
      "stay_minutes": 90,
      "verification_status": "checking"
    }
  ],
  "legs": [
    {
      "from": "start",
      "to": "poi_id",
      "mode": "taxi",
      "duration_minutes": 12,
      "distance_meters": 4300
    }
  ]
}
```

### VerificationTask

```json
{
  "task_id": "string",
  "poi_id": "string",
  "party_size": 2,
  "arrival_time": "18:45",
  "status": "checking",
  "result": {
    "is_open": null,
    "need_wait": null,
    "wait_minutes": null,
    "call_connected": null
  }
}
```

---

## 15. 后端能力拆分

| 服务 | 职责 |
|---|---|
| Intent Service | 本地生活意图识别、字段抽取 |
| Profile Service | 用户画像、历史行为学习 |
| POI Service | 高德/美团 POI 召回与清洗 |
| Ranking Service | 候选 POI 排序 |
| Route Service | 路线计算、时间轴生成 |
| Verification Service | 电话核验任务管理 |
| Session Service | 规划会话状态管理 |

---

## 16. 规划会话状态机

```text
created
→ intent_checked
→ rejected / need_clarification
→ ready_for_candidates
→ candidate_selecting
→ route_generating
→ route_ready
→ verification_updating
→ navigating / reselecting / saved / ended
```

状态规则：
- `rejected`：非本地生活需求。
- `need_clarification`：缺少必填字段。
- `candidate_selecting`：等待用户选择 POI。
- `route_ready`：路线已生成，可执行。
- `verification_updating`：电话核验结果异步回写。
- `reselecting`：局部退出重选。

---

## 17. 核心接口草案

| 接口 | 方法 | 用途 |
|---|---|---|
| `/intent/parse` | POST | 解析用户输入 |
| `/route/session` | POST | 创建规划会话 |
| `/route/session/{id}` | PATCH | 更新补齐条件 |
| `/poi/candidates` | POST | 获取 POI 候选 |
| `/route/generate` | POST | 生成路线 |
| `/verification/request` | POST | 创建电话核验任务 |
| `/verification/{id}` | GET | 查询核验状态 |
| `/route/reselect` | POST | 局部退出重选 |
| `/user/profile` | GET/PATCH | 获取/更新用户偏好 |
| `/history/routes` | GET | 获取历史路线 |

---

## 18. 埋点指标

| 模块 | 指标 |
|---|---|
| 首页 | 输入率、快捷入口点击率、非本地需求比例 |
| 需求补齐 | 补齐完成率、平均追问次数、中途退出率 |
| POI 候选 | 选择率、换一批率、不满意原因分布 |
| 路线结果 | 路线生成成功率、导航点击率、保存率 |
| 电话核验 | 触发率、接通率、未接通率、等位风险比例 |
| 重选 | 局部退出率、替换成功率 |
| 留存 | 次日复用率、7 日复用率、周人均规划次数 |

---

## 19. MVP 验收标准

- 用户能完成从输入需求到路线生成的完整链路。
- 非本地生活需求不会进入路线规划。
- 缺失信息能被补齐，且不重复追问。
- 每个类目能返回最多 3 个高质量候选。
- 用户必须手动选择最终 POI。
- 路线页展示完整时间轴、通勤、预算和风险。
- 电话核验异步更新，不阻塞路线展示。
- 商家未接通时，用户能决定保留或替换。
- 用户能从最后一个 POI 往前局部退出重选。
- 导航、保存、历史复用链路可用。
